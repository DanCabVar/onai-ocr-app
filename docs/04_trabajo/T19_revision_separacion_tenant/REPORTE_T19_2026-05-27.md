# REPORTE T19 ‚Äî Revisi√≥n de separaci√≥n por tenant (2026-05-27)

## Estado

- Tarea: `T19`
- Spec: `docs/03_specs/active/SPEC-19_revision_separacion_tenant.md`
- Estado sugerido: `En revisi√≥n`

## Alcance auditado en esta iteraci√≥n

- Chat SQL/RAG (`backend/src/chat/sql-rag.service.ts`)
- Revisi√≥n de filtros por `userId` en:
  - `backend/src/documents/documents.service.ts`
  - `backend/src/document-types/document-types.service.ts`
  - `backend/src/subscriptions/subscriptions.service.ts`
  - `backend/src/storage/storage.service.ts`
- Validaci√≥n anti-retrabajo en:
  - `docs/PLAN_MAESTRO.md`
  - `docs/06_history/SPEC_HISTORY.md`
  - `docs/03_specs/done/SPEC-DONE-20260527_mc2_ocr_ai_completed.md`

## Hallazgos

1. El chat SQL/RAG ten√≠a `SET LOCAL app.current_user_id` con interpolaci√≥n directa.
2. Faltaba prueba automatizada espec√≠fica para regresi√≥n de aislamiento tenant en SQL/RAG.

## Mitigaciones aplicadas

1. Hardening SQL session context:
   - Cambio de interpolaci√≥n a llamada parametrizada:
   - `SELECT set_config('app.current_user_id', $1, true)`.
2. Restricci√≥n adicional de consultas de chat:
   - Rechazo de queries que no usen `my_documents` o `my_document_types`.
   - Rechazo de acceso directo a tablas base sensibles (`documents`, `document_types`, `subscriptions`, `users`) y cat√°logos del sistema.
3. Pruebas de regresi√≥n agregadas:
   - `backend/src/chat/sql-rag.service.spec.ts`
   - Casos cubiertos:
     - rechazo sin placeholder `$1`;
     - rechazo por tabla directa en vez de vistas;
     - verificaci√≥n de seteo de contexto tenant por `set_config` parametrizado.

## Verificaci√≥n ejecutada

- `cd backend && pnpm test -- sql-rag.service.spec.ts` ‚úÖ
- `cd backend && pnpm run build` ‚úÖ

## Riesgos pendientes

1. Falta a√∫n la validaci√≥n manual end-to-end con 2 tenants reales en ambiente ejecut√°ndose (API/UI + DB logs).
2. Faltan pruebas equivalentes para endpoints no-chat en modalidad integraci√≥n/e2e multiusuario.


## ActualizaciÛn 2026-06-21

### Hallazgo adicional

3. La inferencia de tipos de documento cargaba `document_types` sin filtrar por `userId` en flujos activos y legacy:
   - `inferDocumentTypesFromSamples`
   - `classifyAndGroupDocuments`
   - `createDocumentTypesFromInference`

Esto podÌa mezclar tipos existentes de otros tenants al homologar nombres o al reusar tipos por nombre.

### MitigaciÛn aplicada

4. Hardening en inferencia de tipos:
   - `backend/src/document-types/services/document-type-inference.service.ts`
   - Se restringe la carga de tipos existentes a `where: { userId: user.id }`.
   - Se restringe la b˙squeda legacy por nombre a `where: { name, userId: user.id }`.

5. RegresiÛn automatizada agregada:
   - `backend/src/document-types/services/document-type-inference.service.spec.ts`
   - Casos cubiertos:
     - `inferDocumentTypesFromSamples` consulta tipos solo del usuario actual;
     - `classifyAndGroupDocuments` consulta tipos solo del usuario actual;
     - `createDocumentTypesFromInference` busca coincidencias por nombre dentro del tenant actual.

### VerificaciÛn ejecutada

- `cd backend && pnpm test -- --runInBand src/document-types/services/document-type-inference.service.spec.ts src/chat/sql-rag.service.spec.ts` ?
- `cd backend && pnpm run build` ?

### Estado sugerido tras esta iteraciÛn

- Mantener `T19` en `En revisiÛn`.
- Hardening tÈcnico completado para RAG + inferencia de tipos + storage por prefijo tenant.
- Pendiente para cierre: validaciÛn E2E manual con 2 tenants reales en QA.
- ValidaciÛn QA manual confirmada el 2026-06-21: un usuario nuevo no visualiza los tipos de documento de otro tenant en `https://qa-ocr-app.moti.cl`.
- Incidente QA confirmado el 2026-06-21: en `https://qa-ocr-app.moti.cl/chat` ambos tenants recibÌan `relation "my_documents" does not exist`.
- MitigaciÛn aplicada en backend: `SqlRagService` ahora reescribe `my_documents` y `my_document_types` a subqueries filtradas por `user_id = $1` antes de ejecutar, evitando dependencia operativa de vistas faltantes en la BD.
- RegresiÛn agregada en `backend/src/chat/sql-rag.service.spec.ts` para validar el rewrite seguro previo a ejecuciÛn.
