# REPORTE T19 â€” RevisiÃ³n de separaciÃ³n por tenant (2026-05-27)

## Estado

- Tarea: `T19`
- Spec: `docs/03_specs/active/SPEC-19_revision_separacion_tenant.md`
- Estado sugerido: `En revisiÃ³n`

## Alcance auditado en esta iteraciÃ³n

- Chat SQL/RAG (`backend/src/chat/sql-rag.service.ts`)
- RevisiÃ³n de filtros por `userId` en:
  - `backend/src/documents/documents.service.ts`
  - `backend/src/document-types/document-types.service.ts`
  - `backend/src/subscriptions/subscriptions.service.ts`
  - `backend/src/storage/storage.service.ts`
- ValidaciÃ³n anti-retrabajo en:
  - `docs/PLAN_MAESTRO.md`
  - `docs/06_history/SPEC_HISTORY.md`
  - `docs/03_specs/done/SPEC-DONE-20260527_mc2_ocr_ai_completed.md`

## Hallazgos

1. El chat SQL/RAG tenÃ­a `SET LOCAL app.current_user_id` con interpolaciÃ³n directa.
2. Faltaba prueba automatizada especÃ­fica para regresiÃ³n de aislamiento tenant en SQL/RAG.

## Mitigaciones aplicadas

1. Hardening SQL session context:
   - Cambio de interpolaciÃ³n a llamada parametrizada:
   - `SELECT set_config('app.current_user_id', $1, true)`.
2. RestricciÃ³n adicional de consultas de chat:
   - Rechazo de queries que no usen `my_documents` o `my_document_types`.
   - Rechazo de acceso directo a tablas base sensibles (`documents`, `document_types`, `subscriptions`, `users`) y catÃ¡logos del sistema.
3. Pruebas de regresiÃ³n agregadas:
   - `backend/src/chat/sql-rag.service.spec.ts`
   - Casos cubiertos:
     - rechazo sin placeholder `$1`;
     - rechazo por tabla directa en vez de vistas;
     - verificaciÃ³n de seteo de contexto tenant por `set_config` parametrizado.

## VerificaciÃ³n ejecutada

- `cd backend && pnpm test -- sql-rag.service.spec.ts` âœ…
- `cd backend && pnpm run build` âœ…

## Riesgos pendientes

1. Falta aÃºn la validaciÃ³n manual end-to-end con 2 tenants reales en ambiente ejecutÃ¡ndose (API/UI + DB logs).
2. Faltan pruebas equivalentes para endpoints no-chat en modalidad integraciÃ³n/e2e multiusuario.


## Actualización 2026-06-21

### Hallazgo adicional

3. La inferencia de tipos de documento cargaba `document_types` sin filtrar por `userId` en flujos activos y legacy:
   - `inferDocumentTypesFromSamples`
   - `classifyAndGroupDocuments`
   - `createDocumentTypesFromInference`

Esto podía mezclar tipos existentes de otros tenants al homologar nombres o al reusar tipos por nombre.

### Mitigación aplicada

4. Hardening en inferencia de tipos:
   - `backend/src/document-types/services/document-type-inference.service.ts`
   - Se restringe la carga de tipos existentes a `where: { userId: user.id }`.
   - Se restringe la búsqueda legacy por nombre a `where: { name, userId: user.id }`.

5. Regresión automatizada agregada:
   - `backend/src/document-types/services/document-type-inference.service.spec.ts`
   - Casos cubiertos:
     - `inferDocumentTypesFromSamples` consulta tipos solo del usuario actual;
     - `classifyAndGroupDocuments` consulta tipos solo del usuario actual;
     - `createDocumentTypesFromInference` busca coincidencias por nombre dentro del tenant actual.

### Verificación ejecutada

- `cd backend && pnpm test -- --runInBand src/document-types/services/document-type-inference.service.spec.ts src/chat/sql-rag.service.spec.ts` ?
- `cd backend && pnpm run build` ?

### Estado sugerido tras esta iteración

- Mantener `T19` en `En revisión`.
- Hardening técnico completado para RAG + inferencia de tipos + storage por prefijo tenant.
- Pendiente para cierre: validación E2E manual con 2 tenants reales en QA.
- Validación QA manual confirmada el 2026-06-21: un usuario nuevo no visualiza los tipos de documento de otro tenant en `https://qa-ocr-app.moti.cl`.
- Incidente QA confirmado el 2026-06-21: en `https://qa-ocr-app.moti.cl/chat` ambos tenants recibían `relation "my_documents" does not exist`.
- Mitigación aplicada en backend: `SqlRagService` ahora reescribe `my_documents` y `my_document_types` a subqueries filtradas por `user_id = $1` antes de ejecutar, evitando dependencia operativa de vistas faltantes en la BD.
- Regresión agregada en `backend/src/chat/sql-rag.service.spec.ts` para validar el rewrite seguro previo a ejecución.
- Validación QA manual confirmada el 2026-06-21 en `https://qa-ocr-app.moti.cl/chat`: el tenant con 10 documentos recibe respuesta consistente (`10`) y el tenant sin documentos recibe respuesta consistente (`no tiene ninguno`).
- Con esto queda verificado en QA que el chat SQL/RAG responde sin fuga cruzada visible entre ambos tenants para conteo básico de documentos.
- Estado final sugerido el 2026-06-21: `Cerrada`.
- Criterios validados en QA: aislamiento de tipos por tenant, aislamiento visible del chat por tenant y conteos consistentes sin fuga cruzada.
